import React from 'react';
import ReactDOM from 'react-dom/client';
import MiloCostCalculator from '../kodiak-cost-calculator.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MiloCostCalculator />
  </React.StrictMode>
);
